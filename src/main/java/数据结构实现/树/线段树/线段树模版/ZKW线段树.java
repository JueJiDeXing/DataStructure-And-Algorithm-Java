package 数据结构实现.树.线段树.线段树模版;

public class ZKW线段树 {

    //void build(int n) {
    //    int m = 1 << n;
    //    for (int i = m + 1; i <= m + n; ++i) op_array[i] = read();
    //    for (int i = m - 1; i > 0; --i) operation(i);
    //}
    //
    //int[] sum;
    //
    //void operation(int i) {
    //    sum[i] = sum[i << 1] + sum[i << 1 | 1];
    //}
}
